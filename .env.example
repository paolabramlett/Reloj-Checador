# Supabase — copiar a .env.local y llenar con los valores del proyecto
# (Dashboard → Settings → API, o `supabase status` si corre localmente)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor (ingesta y operaciones administrativas). NUNCA exponer al cliente.
SUPABASE_SERVICE_ROLE_KEY=
